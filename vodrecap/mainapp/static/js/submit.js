// TODO:
// 1. Sanitize input
// 2. Send input to API
// 3. Store input from API as JSON
// 4. Create Visualizations


function submit_url() {
    // Fetch input in #search-box DOM element
    url= $("#search-box").val();
    console.log(url);
}

function sanitize_url(url) {
    return
}

function submit_url_to_api(url) {
    return
}
